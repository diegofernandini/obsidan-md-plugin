# builtin-modules
#tag: Nodejs, Built-in modules, Bibliotecas

> A static list of the Node.js builtin modules from the latest Node.js version.

The list is just a [JSON file](builtin-modules.json) and can be used anywhere.

## Install
 Instalar la biblioteca mediante npm:
```sh
npm install builtin-modules
```

## Usage
Importar y utilizar la lista de módulos internos de Node.js:

```js
import builtinModules from 'builtin-modules';

console.log(builtinModules);
//=> ['node:assert', 'assert', 'node:buffer', 'buffer', …]
```

## Tip
Utilizar la API built-in para obtener la lista de módulos del versión actual de Node.js:
```js
import {builtinModules} from 'node:module';
```

## Related
- [[is-builtin-module]] - Verificar si una cadena coincide con el nombre de un módulo integrado de Node.js

> [!info] Nota importante: esta lista es estática y solo se actualiza cuando se actualiza la versión de Node.js.