```yml
---
tags: ['arquitectura', 'react', 'dom']
---

# react-dom

Este paquete funciona como punto de entrada para los renderizadores DOM y servidor de React. Se pretende utilizar en conjunción con el paquete genérico de React, que se envía a npm como `react`.

## Instalación

```sh
npm install react react-dom
```

## Uso

### En el navegador

```js
import { createRoot } from 'react-dom/client';

function App() {
  return <div>Hello World</div>;
}

const root = createRoot(document.getElementById('root'));
root.render(<App />);
```

### En servidor

```js
import { renderToPipeableStream } from 'react-dom/server';

function App() {
  return <div>Hello World</div>;
}

function handleRequest(res) {
  // ... en tu manejo de solicitudes del servidor ...
  const stream = renderToPipeableStream(<App />, {
    onShellReady() {
      res.statusCode = 200;
      res.setHeader('Content-type', 'text/html');
      stream.pipe(res);
    },
    // ...
  });
}
```

## API

### `react-dom`

 Consulta https://react.dev/reference/react-dom

### `react-dom/client`

 Consulta https://react.dev/reference/react-dom/client

### `react-dom/server`

 Consulta https://react.dev/reference/react-dom/server